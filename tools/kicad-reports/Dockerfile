FROM kicad/kicad:10.0.4

# The upstream image already runs as a non-root "kicad" user with uid 1000
# (a typical single-user Linux workstation uid), so output files land
# owned by the host user without any extra setup here.
